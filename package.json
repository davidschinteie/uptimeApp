{
  "name": "uptime-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "node-sass": "^4.12.0",
    "npm-run-all": "^4.1.5",
    "react": "^16.9.0",
    "react-dom": "^16.9.0",
    "react-scripts": "3.1.2"
  },
  "scripts": {
    "start-js": "react-scripts start",
    "start": "npm-run-all -p start-js",
    "build-js": "react-scripts build",
    "build": "npm-run-all build-css build-js",
    "build-dev": "REACT_APP_ENV=development npm-run-all build-css build-js",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "npm run build && rsync --delete --recursive --progress ./build/* root@172.105.73.116:/home/okapiuptime/public"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "axios": "^0.19.0",
    "bulma": "^0.7.5",
    "prop-types": "^15.7.2",
    "react-modal": "^3.10.1"
  }
}